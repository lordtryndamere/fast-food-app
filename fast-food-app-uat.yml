server:
  port: 8081
defaultnames:
  names:
    name: defaultNameUAT
    description: defaultDescriptionUAT
enable-audit: true
#habilita los endpoints de actuator
management:
  endpoints:
    web:
      exposure:
        include: "*"
